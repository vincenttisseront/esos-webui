<Page
    x:Class="GpoManager.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="16">
            <TextBlock Text="Tableau de bord" Style="{ThemeResource TitleLargeTextBlockStyle}"/>
            <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red"/>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="GPO total" Style="{ThemeResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.TotalGpos, Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Orphelins" Style="{ThemeResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.OrphanedGpos, Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Conflits" Style="{ThemeResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.ConflictCount, Mode=OneWay}" Style="{ThemeResource TitleLargeTextBlockStyle}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Dernière analyse" Style="{ThemeResource CaptionTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.LastAnalysis, Mode=OneWay}" Style="{ThemeResource BodyTextBlockStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Button Content="Actualiser" Command="{x:Bind ViewModel.LoadCommand}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
